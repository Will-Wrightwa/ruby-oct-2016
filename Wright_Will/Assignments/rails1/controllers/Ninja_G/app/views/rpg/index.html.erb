<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Ninja Gold</title>
        <link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">
    </head>
    <body>
        <div class="container">


            <h4>Your Gold: <%=session[:gold]%></h4>
            <div class="row">
                <% @places.each do |key, value| %>
                    <form action="/earn" method="post" class="col-lg-3 col-md-3 col-sm-3 ">
                        <h4><%= key %></h4>
                        <h6> Earns <%= value.first %> - <%=value.last%></h6>
                        <input name="authenticity_token" value="<%= form_authenticity_token %>" type="hidden">
                        <input type="hidden" name="location" value="<%=key%>">
                        <input type="submit" name="submit" value="Find Gold!">
                    </form>
                <% end %>
            </div>
            <h5>Activites:</h5>
            <div class="log">
                <% session[:log].reverse.each do |log| %>
                    <% if log["earnings"] > 0 %>
                        <h6 class="green"> Earned  <%=log["earnings"]%> from the <%=log["location"]%> <%=log["time"]%> </h6>
                    <% else %>
                        <h6 class="red"> Lost  <%=log["earnings"].abs%> ffrom the <%=log["location"]%> <%=log["time"]%></h6>
                    <% end %>
                <% end %>
            </div>
        </div>
    </body>
</html>
